// Numerico - NUMBER
    let edad = 34;
    let precio = 150.69;

// Cadena de texto - STRING
    let nombre = 'Bruce';
    let apellido = "Wayne";

// Logicos o booleanos - BOOLEAN
    let leGustaJavascript = true;
    let leGustaFrontEnd = false;

// Objeto (coleccion de datos) - OBJECT
    let persona = {
        nombre: "Maria",
        apellido: "Arteaga",
        edad: 23,
        leGustaElRock: true
    }

// Arreglo (tipo especial de objeto) - ARRAY
    let instrumentos = ["Piano", "Guitarra", "Bateria", 24, true]

//DATOS ESPECIALES:
    let malaDivision = "35" / 2; //NaN (No es un numero)
    let temperatura = null; //No llego ningun dato, algo fallo.
    let otraVariable; //UNDEFINED, una variable sin definir.

